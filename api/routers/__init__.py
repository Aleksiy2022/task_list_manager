"""
Package 'routers'.

Components of the package.
1. The 'auth' module provides logic for processing URL
    requests with the 'auth' prefix.
2. The 'tasks' module provides logic for processing URL
    requests with the 'task' prefix.
"""
